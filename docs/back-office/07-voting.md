# Voting tags taxonomy + move vote types to the DB

**Milestone:** 🚪 Back-office and arena customisation (#17)
**Related:** no dedicated milestone yet.
**Labels:** front, arena

## Mockup

> 📎 Attach `05 _ Content _ Voting.png` here.

## Summary

Configure the tags users attach to a vote to explain why they preferred an answer — positive and negative lists, each editable and reorderable.

## Scope (to detail later)

- Positive + negative tag lists (label + emoji), add/edit/remove, drag-to-reorder.
- **Foundational: move vote tag taxonomy into the DB** instead of hardcoding it, so it's editable per instance.

## Notes

Changing the taxonomy affects how votes are recorded and later published in the dataset — coordinate with the data-publishing schema.
