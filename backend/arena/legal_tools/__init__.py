"""
Agentic legal skills and tools for the compar:IA juridique instance.

- "Skills" are static instructions appended to the system prompt (no model
  call-time logic involved).
- "Tools" (surfaced to users as "MCP servers") are functions the model can
  choose to invoke mid-response via standard OpenAI-style tool-calling.
"""
